<!DOCTYPE html>
<html>
<head>
    <title>Water Bill Payment</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="card">
        <div class="card-header">
            <h1 class="h3">Find Your Water Bill</h1>
        </div>
        <div class="card-body">
            <% if (error) { %>
                <div class="alert alert-danger"><%= error %></div>
            <% } %>
            <form action="/find-bill" method="POST">
                <div class="form-group">
                    <label for="billId" class="form-label">Enter Bill ID:</label>
                    <input type="text" id="billId" name="billId" class="form-control" placeholder="e.g., 101" required>
                </div>
                <button type="submit" class="btn btn-primary">Find Bill</button>
            </form>
        </div>
    </div>
</body>
</html>